<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../layout/head') %>
  <title>Playlist</title>
</head>

<body>
  <header>

  <div id ="header"><h1>GAME NIGHT</h1></div>
  
  <div class="navbar">
  <div class = "nav">
  
  <div id="home"><a href="/"><img src="../img/homebutton.png"></a></div>
  <div id="about"><a href="/aboutus"><img src="../img/aboutbutton.png"></a></div>
  <div id="fav"><a href="/favorites"><img src="../img/favbutton1.png"></a></div>
  <div id="playlist-nav"><a href="/playlist"><img src="../img/playlist-button-nav.png"></a></div>
  <div id="boardgame-nav"><a href="/boardgames"><img src="../img/boardgame-button-nav.png"></a></div>
  <div id="trivia-nav"><a href="/trivia"><img src="../img/trivia-button-nav.png"></a></div>
 </div>
</div>
</header>
<div id = "playListHeader"><h1>PLAYLISTS</h1></div>

    <%- include('../layout/header') %>
  </header>
 
  <div class="container">
    <form action="/playlist" method="POST">
      <input type="text" name="search" placeholder="Enter search phrase">
      <button type="submit">Search</button>
    </form>

    <% if (typeof(playlists) !== 'undefined') { %>
    <% playlists.forEach(playlist => { %>

    <section id="search-result">

      <h2><%= playlist.name %></h2>
      <img class ="playlist-img" src="<%= playlist.image %>"></img>
      <span><iframe src="https://open.spotify.com/embed/playlist/<%= playlist.spotifyId %>" width="300" height="380"
        frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe></span>

      <form action="/favorites/playlist" method="POST">
        <input type="hidden" name="name" value="<%= playlist.name %>">
        <input type="hidden" name="description" value="<%= playlist.description %>">
        <input type="hidden" name="url" value="<%= playlist.url %>">
        <input type="hidden" name="image" value="<%= playlist.image %>">
        <input type="hidden" name="spotifyId" value="<%= playlist.spotifyId %>">
        <button type="submit">Save Playlist</button>
      </form>

    </section>
    <% }) %>
    <% } %>

  </div>

  <!-- <footer>
    <div id ="footer"></div>
  </footer> -->
</body>

</html>